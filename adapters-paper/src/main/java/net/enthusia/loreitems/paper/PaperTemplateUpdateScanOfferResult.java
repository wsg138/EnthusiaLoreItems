package net.enthusia.loreitems.paper;

/** Result of offering a naturally accessible inventory to the bounded scan backlog. */
enum PaperTemplateUpdateScanOfferResult {
    READY,
    DEFERRED,
    ALREADY_QUEUED,
    REJECTED
}
