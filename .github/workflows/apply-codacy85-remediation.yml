name: Apply Codacy 85 remediation

on:
  pull_request:

permissions:
  contents: write

jobs:
  apply:
    if: github.event.pull_request.head.repo.full_name == github.repository && github.event.pull_request.head.ref == 'agent/loreitems-pr1-foundation'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
      - name: Apply reviewed remediation
        env:
          HEAD_BRANCH: ${{ github.event.pull_request.head.ref }}
        run: |
          set -euo pipefail
          cat .github/codacy85.patch.00 .github/codacy85.patch.01 .github/codacy85.patch.02 .github/codacy85.patch.03 .github/codacy85.patch.04 \
            | base64 --decode \
            | gzip --decompress \
            > "$RUNNER_TEMP/codacy85.patch"
          git apply --check "$RUNNER_TEMP/codacy85.patch"
          git apply "$RUNNER_TEMP/codacy85.patch"
          rm .github/codacy85.patch.00 \
             .github/codacy85.patch.01 \
             .github/codacy85.patch.02 \
             .github/codacy85.patch.03 \
             .github/codacy85.patch.04 \
             .github/workflows/apply-codacy85-remediation.yml \
             .github/workflows/codacy-target-evidence.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add --all
          git diff --cached --check
          git commit -m "resolve remaining Codacy findings"
          git push origin "HEAD:${HEAD_BRANCH}"
