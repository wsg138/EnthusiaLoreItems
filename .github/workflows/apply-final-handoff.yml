name: Apply final handoff documentation

on:
  pull_request:

permissions:
  contents: write

jobs:
  apply:
    if: github.event.pull_request.head.repo.full_name == github.repository && github.event.pull_request.head.ref == 'agent/loreitems-pr1-foundation'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
      - name: Apply reviewed handoff documentation
        env:
          HEAD_BRANCH: ${{ github.event.pull_request.head.ref }}
        run: |
          set -euo pipefail
          base64 --decode .github/handoff-docs.patch.gz.b64 | gzip --decompress > "$RUNNER_TEMP/handoff-docs.patch"
          git apply --check "$RUNNER_TEMP/handoff-docs.patch"
          git apply "$RUNNER_TEMP/handoff-docs.patch"
          rm .github/handoff-docs.patch.gz.b64 .github/workflows/apply-final-handoff.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add --all
          git diff --cached --check
          git commit -m "document PR 2 Codacy remediation and merge readiness"
          git push origin "HEAD:${HEAD_BRANCH}"
